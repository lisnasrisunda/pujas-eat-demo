import 'package:pujas_eat_demo/src/model.dart' as m;
import 'package:pujas_eat_demo/src/controller.dart';

class TenanController extends ControllerMVC {
  // List<m.TenanModel> getTenanById(String idTenan) {
  //   List<m.TenanModel> output = [];
  //   m.TenanModel.getTenanById(idTenan).then((tenan) {
  //     for (int i = 0; i < tenan.length; i++) 
  //       output.add(tenan[i]);

  //     setState(() {});
  //   });

  //   return(output);
  // }
}